export default class Routing {
    constructor(sourceNode) {
        /** {} */
        this.sourceNode = sourceNode;
        /**
         * @type {Array}
         */
        this.outputs = [];
    }
}